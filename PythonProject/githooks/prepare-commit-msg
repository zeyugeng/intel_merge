#!/bin/sh
# Remove Cursor attribution lines from commit messages.
MSG_FILE="$1"
[ -f "$MSG_FILE" ] || exit 0

TMP_FILE="${MSG_FILE}.cursor-clean"
grep -v -E '^(Co-authored-by: Cursor|Made-with: Cursor)' "$MSG_FILE" > "$TMP_FILE"
mv "$TMP_FILE" "$MSG_FILE"
